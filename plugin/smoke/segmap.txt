# plugin/smoke — the whole test kernel is one segment. virt RAM starts at
# 0x40000000; the kernel links at 0x40080000. QEMU's built-in boot shim runs
# from somewhere else and lands in `other`, which the assertions ignore.
40000000 50000000 shell
